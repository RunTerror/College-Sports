import 'package:flutter/material.dart';

class ServerSceen extends StatelessWidget {
  const ServerSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Sever Screen"),),);
  }
}